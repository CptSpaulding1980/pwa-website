{
  "name": "pwa-website",
  "version": "1.0.0",
  "description": "PWA - Pro Wrestling Action static website",
  "scripts": {
    "build": "npx @11ty/eleventy",
    "serve": "npx @11ty/eleventy --serve"
  },
  "devDependencies": {
    "@11ty/eleventy": "^2.0.1"
  },
  "dependencies": {
    "markdown-it": "^14.1.0"
  }
}
