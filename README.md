# PWA — Pro Wrestling Action

Static archive of the parallel wrestling universe. 234 events, 269 wrestlers, 4 championships from 1982–2019.

**Build:** `python3 build.py` — pulls data from Obsidian vault, generates static HTML in `docs/`.

**Deploy:** Push to main branch. GitHub Pages serves from `/docs`.
